{
  "name": "@drever/website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp build && tsc --noEmit",
    "deploy": "pnpm --dir .. run build:website && wrangler pages deploy dist/client --project-name drever-website --branch main",
    "dev": "vp dev",
    "preview": "vp preview",
    "preview:pages": "pnpm --dir .. run build:website && wrangler pages dev dist/client --port 4318 --compatibility-date 2026-07-22",
    "test": "vp test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@drever/brand": "workspace:*",
    "@tanstack/react-router": "catalog:",
    "@tanstack/react-start": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@mdx-js/rollup": "catalog:",
    "@shikijs/rehype": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "remark-gfm": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "wrangler": "catalog:"
  },
  "engines": {
    "node": ">=24.18.0"
  }
}
