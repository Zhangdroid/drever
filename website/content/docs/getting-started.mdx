<h2 id="what-the-agent-does">What the handoff completes</h2>

1. **Prepare** — creates a safe project and installs its version-matched Codex and Claude Code
   workflows.
2. **Ask** — opens an experimental local creation room for the topic, common choices, and a few
   questions chosen from the subject and your earlier answers.
3. **Storyboard** — writes the complete brief and machine-checkable story contract, renders the
   page-by-page plan, and stops for your approval.
4. **Create** — turns the approved plan into readable MDX, speaker notes, subject-led art direction,
   and intentional motion, then opens one coherent Draft 1 in the real presentation runtime.
5. **Refine and deliver** — checks every rendered state, improves the same live draft, and only then
   builds the production presentation or requested PDF.

The public [`prompt.md`](/prompt.md) only starts the work. Once the project exists, its local
instructions and skills take over so the agent always follows the contract for the installed Drever
version.

When browser control and a persistent local server are available, the agent prefers the local
creation room. Enter the topic, audience outcome, duration, visible slide density, and motion
intensity there. The agent then returns one to three subject-specific questions about evidence,
scope, story, visuals, or delivery. Each option explains what it changes, accepts free-form input,
and keeps **Skip remaining questions — surprise me** available. A later round can respond to an
earlier answer instead of repeating a fixed questionnaire.

The agent then writes the resolved direction and numbered outline to `brief.md`, records every
slide's job, evidence, focal artifact, composition, density, and motion ownership in
`drever.plan.json`, runs the fast source check, and shows the visual Storyboard inside the same room
before stopping for approval. The escape delegates unanswered choices but does not bypass this
review. If the room is unavailable or you prefer chat, the agent asks the same decisions in the
conversation and shares the exact read-only `/storyboard` URL instead.

After approval, the agent publishes a complete, valid Draft 1 rather than streaming partial MDX
into the canvas. The real Audience View appears inside the creation room, where feedback can target
one slide or the whole deck. The work still happens in ordinary files on your machine: `slides.mdx`,
`brief.md`, `drever.plan.json`, assets, and Git remain the source of truth.

The first URL is intentionally a progress milestone, not a claim that the deck is finished. Review
the story, facts, and emphasis immediately while the agent continues checking readability, layout,
motion, Steps, and browser behavior. Feedback can arrive during that pass; the agent updates the
same live preview, discards stale checks, and delays the production build until the refined preview
is stable. Authoring and design use source-only and affected-route checks during iteration; review
owns the exhaustive rendered gate, and delivery reuses fresh review evidence for the final build or
requested export.

<h2 id="local-workflow">Know what runs where</h2>

The website button copies text; it does not upload the brief or start a hosted generation job. Paste
the prompt into the coding agent you already trust. That agent creates a local React and MDX
project, installs packages from npm, and may use the network for research only when the brief and
your agent permissions allow it.

The creation room is experimental, development-only, and local to the project. It coordinates the
coding agent that is already running; it does not call a model itself, store API keys or provider
transcripts, add an editor to production, or become part of the audience bundle.

Drever does not add telemetry to the generated project. Your chosen AI provider, any remote media,
and the platform where you later publish the static build each keep their own privacy contract.

<h2 id="write-a-useful-brief">Give the agent a useful outcome</h2>

A single sentence is enough to begin. Include the audience, the change you want, and any required
source material when you know them:

> Create a short presentation for local residents about three proposals for a shared public space.
> Help them understand the trade-offs and decide which proposal to support. Use the planning brief
> and survey results as source material.

The agent can infer ordinary details and state its assumptions. It marks at most one option as
recommended when your brief supports a clear default. After the topic is known, say **Skip remaining
questions — surprise me** in any round to let it resolve every unanswered choice, answer with option
letters, combine options, or reply in your own words while the questions remain useful.
