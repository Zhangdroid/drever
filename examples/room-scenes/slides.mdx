import { OpeningRoom } from "./OpeningRoom.tsx";
import "@drever/brand/fonts.css";
import "@drever/brand/tokens.css";
import "@drever/scenes/styles.css";
import "./room-scenes.css";
import "../showcase-return.css";

<OpeningRoom aria-label="Before the room speaks" />

<Note>
  Enable the microphone, then play music through the computer speakers. The low, mid, and high
  frequencies shape the persistent glow and rings without putting the background inside the slide
  transition. The captured audio stays inside the browser and is released when listening stops.
</Note>

---

<div aria-label="The background has its own job" className="stage-story">
  <header>
    <span className="scene-kicker">Persistent atmosphere</span>
    <h2>The background has its own job.</h2>
    <p>The glow behind this deck is a Stage scene. It stays mounted while the slide content changes, then shifts only when the story gives it a reason.</p>
  </header>

  <div className="stage-story__model" aria-label="A persistent Stage surrounding changing slides">
    <span>Stage · persistent</span>
    <div>
      <small>Slide · changing</small>
      <strong>Content keeps the attention.</strong>
    </div>
    <i>quiet signal</i>
  </div>
</div>

<Note>
  The global field has moved from gathering around the opening to a quieter focus state. It is not
  part of the slide View Transition, so unchanged pixels do not flash or leave captured ghosts.
</Note>

---

<div aria-label="Every Scene needs a narrative job" className="scene-board">
  <header>
    <span className="scene-kicker">Not a component zoo</span>
    <h2>Every Scene needs a narrative job.</h2>
  </header>

<ol className="scene-board__moments" aria-label="Incubating Scenes by narrative moment">
  <li>
    <span>While the room is alive</span>
    <strong>RoomAudio</strong>
    <p>Let music, voices, or applause reaching the microphone shape a local visual signal.</p>
  </li>
  <li>
    <span>While the story moves</span>
    <strong>AmbientStage</strong>
    <p>Carry one visual atmosphere across the deck without moving every page.</p>
  </li>
  <li>
    <span>At the shared start</span>
    <strong>RoomCountdown</strong>
    <p>Give a live room a clear start while exports remain deterministic.</p>
  </li>
</ol>

  <p className="scene-board__roadmap">
    <span>On the roadmap</span>
    {"BrowserJourney · CompareLens · AudiencePulse"}
  </p>
</div>

<Note>
  The package stays deliberately narrow. BrowserJourney, CompareLens, DataStory, MediaCue, and an
  audience pulse are stronger next candidates than generic cards or buttons.
</Note>

---

<div aria-label="The room is part of the story" className="scene-ending">
  <span className="scene-kicker">Drever / presentation-native scenes</span>
  <h2>The room is part of the story.</h2>
  <p>
    {
      "Slides carry the argument. Scenes shape the moments around it—only when those moments matter."
    }
  </p>
  <nav aria-label="Continue with Drever" className="drever-example-exit">
    <a
      className="drever-example-exit__primary"
      data-drever-showcase-return=""
      href="https://drever.dev/docs/getting-started/"
    >
      <span>Start with Drever</span>
      <span aria-hidden="true">↗</span>
    </a>
    <a className="drever-example-exit__secondary" href="https://drever.dev/showcase/">
      <span>Explore the showcase</span>
      <span aria-hidden="true">↗</span>
    </a>
  </nav>
  <div className="scene-ending__signal" aria-hidden="true">
    <i />
    <i />
    <i />
  </div>
</div>

<Note>
  The opening signal returns in a smaller, quieter form to close the loop. One visual callback is
  enough; the final page does not introduce another effect. Leave the room with a direct path to the
  getting-started guide and the wider showcase.
</Note>
