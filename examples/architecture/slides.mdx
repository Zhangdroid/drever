import { ArtifactExplorer, RouteCompiler } from "./ArchitectureLab.tsx";
import "./architecture.css";

<Statement
  eyebrow="Drever / architecture"
  index="00"
  title="One source. Many trustworthy surfaces."
  supporting="The architecture turns authored intent into a stable contract before it becomes a presentation."
/>

<Note>
  Frame this as an architecture of agreements, not packages. Each layer exists to make audience,
  speaker view, build output, tests, and future tools agree on what the deck means.
</Note>

---

## Start with invariants—not components.

<div className="arch-principles">
  <article>
    <span>01 / semantics</span>
    <strong>Meaning before rendering</strong>
    <p>The deck is serializable before React enters the picture.</p>
  </article>
  <article>
    <span>02 / navigation</span>
    <strong>URL as source of truth</strong>
    <p>History, reload, sync, and tests share one position.</p>
  </article>
  <article>
    <span>03 / extensions</span>
    <strong>Capability with provenance</strong>
    <p>Every contribution has an owner and a validated boundary.</p>
  </article>
  <article>
    <span>04 / failure</span>
    <strong>Errors as data</strong>
    <p>Stable diagnostics serve people, tools, and AI equally.</p>
  </article>
</div>

<Note>
  These invariants explain most implementation decisions later in the deck. They are deliberately
  independent of any specific component library or bundler.
</Note>

---

## The deck becomes data before UI.

<ArtifactExplorer />

<Note>
  Move through all four layers. Deck IR is the semantic center; the manifest is the frozen runtime
  projection. The viewer never discovers Steps or notes by inspecting rendered DOM.
</Note>

---

## Compilation is an owned sequence.

<MotionGroup className="arch-pipeline" intent="focus">
  <Step at={1}>
    <article>
      <span>01 / grammar</span>
      <strong>Split</strong>
      <p>Reserve root-level `---` before extensions run.</p>
    </article>
  </Step>
  <Step at={2}>
    <article>
      <span>02 / model</span>
      <strong>Analyze</strong>
      <p>Build Deck IR with exact source fragments.</p>
    </article>
  </Step>
  <Step at={3}>
    <article>
      <span>03 / plan</span>
      <strong>Resolve</strong>
      <p>Validate theme, plugins, order, and provenance.</p>
    </article>
  </Step>
  <Step at={4}>
    <article>
      <span>04 / transform</span>
      <strong>Compile</strong>
      <p>Run protected Remark, Rehype, and Recma phases.</p>
    </article>
  </Step>
  <Step at={5}>
    <article>
      <span>05 / artifact</span>
      <strong>Seal</strong>
      <p>Emit MDX, manifest, registry, styles, and setup.</p>
    </article>
  </Step>
</MotionGroup>

<Note>
  Advance five times. Boundary detection precedes extension transformations so plugin order cannot
  change pagination. Final validation seals the framework-owned structure after each extension
  phase.
</Note>

---

<Workbench
  label="Theme and plugin answer different questions."
  main={
    <div className="arch-boundaries">
      <section className="arch-boundary">
        <div className="arch-boundary__header">
          <h3>Theme</h3>
          <code>visual language</code>
        </div>
        <ul>
          <li>Design tokens</li>
          <li>Markdown elements</li>
          <li>Layouts and motion grammar</li>
        </ul>
      </section>
      <section className="arch-boundary">
        <div className="arch-boundary__header">
          <h3>Plugin</h3>
          <code>added capability</code>
        </div>
        <ul>
          <li>Remark / Rehype / Recma</li>
          <li>Vite build modules</li>
          <li>Components and lifecycle hooks</li>
        </ul>
      </section>
    </div>
  }
  rail={
    <div className="arch-boundary__rule">
      <strong>The boundary</strong>
      <p>
        {
          "Changing a theme must not change content semantics. A feature that does belongs in a plugin."
        }
      </p>
    </div>
  }
/>

<Note>
  Components can exist in both, but ownership resolves the apparent overlap. Theme components are
  semantic composition vocabulary; plugin components are the runtime face of an added capability.
</Note>

---

## The URL is presentation state.

<RouteCompiler />

<Note>
  Change surface, slide, and Step. The lab mirrors a selected projection of this deck's compiled
  manifest. The encoder is deterministic and production materializes only valid positions.
</Note>

---

## Navigation is one commit protocol.

<div className="arch-commit">
  <div>
    <span>01</span>
    <strong>Intercept</strong>
    <p>Navigation API receives intent.</p>
  </div>
  <div>
    <span>02</span>
    <strong>Decode</strong>
    <p>Path resolves to a manifest position.</p>
  </div>
  <div>
    <span>03</span>
    <strong>Classify</strong>
    <p>Step, slide, or jump direction.</p>
  </div>
  <div>
    <span>04</span>
    <strong>Transition</strong>
    <p>The canvas captures old and new.</p>
  </div>
  <div>
    <span>05</span>
    <strong>Commit</strong>
    <p>React exposes one coherent state.</p>
  </div>
  <div>
    <span>06</span>
    <strong>Publish</strong>
    <p>Speaker and audience synchronize.</p>
  </div>
</div>

<div className="arch-scope">
  <strong>Element-scoped by design</strong>
  <code>canvas.startViewTransition(&#123; update, types &#125;)</code>
</div>

<Note>
  This protocol prevents the URL, React tree, animation, and remote audience from briefly
  disagreeing. The transition root is the slide canvas, so stage chrome is not part of either
  captured frame.
</Note>

---

## Static output materializes the state space.

<div className="arch-static">
  <div className="arch-static__tree">
    <strong>dist/</strong>
    <span>├── index.html</span>
    <span>├── 2/index.html</span>
    <span>├── 4/5/index.html</span>
    <span>├── speaker/index.html</span>
    <span>└── speaker/4/5/index.html</span>
  </div>
  <div className="arch-static__contract">
    <div>
      <strong>Known depth</strong>
      <p>Each entry computes its deployment mount before assets activate.</p>
    </div>
    <div>
      <strong>Portable links</strong>
      <p>Slash and no-slash directory URLs resolve to one canonical position.</p>
    </div>
    <div>
      <strong>No app server</strong>
      <p>A static host can reload every audience and speaker deep link.</p>
    </div>
  </div>
</div>

<Note>
  This is generation, not an SPA fallback. The tiny bootstrap establishes the absolute base before
  deferred assets load, including when the whole deck is mounted below a subdirectory.
</Note>

---

## Resources unwind. Failures stay legible.

<div className="arch-lifecycle">
  <div className="arch-resource-stack">
    <span className="arch-kicker">Lifecycle stack</span>
    <ol>
      <li>Navigation</li>
      <li>Keyboard</li>
      <li>Synchronization</li>
      <li>Plugin setup</li>
    </ol>
    <small>Abort once · dispose in reverse · roll back partial setup</small>
  </div>
  <div className="arch-diagnostic">
    <code>DREVER_COMPILE_STEP_INVALID</code>
    <strong>Step `at` must be a positive static integer.</strong>
    <div className="arch-diagnostic__location">slides.mdx:18:7 · compiler / analyze</div>
    <p>
      {
        "Expected problems cross boundaries as serializable diagnostics. Unexpected invariants are caught once at the adapter edge."
      }
    </p>
  </div>
</div>

<Note>
  The setup runner treats acquisition and disposal as a transaction. Diagnostics have stable codes,
  stage, owner, source range, and an actionable hint—the same structure works in a CLI, overlay,
  test, or AI repair loop.
</Note>

---

## Test each promise where it can fail.

<div className="arch-testing">
  <div>
    <strong>Pure state</strong>
    <p>Routes, transition classification, ordering, and diagnostics.</p>
    <span>unit</span>
  </div>
  <div>
    <strong>Artifact contract</strong>
    <p>Deck IR and manifest semantics—not generated JavaScript snapshots.</p>
    <span>compiler</span>
  </div>
  <div>
    <strong>User reality</strong>
    <p>
      {"Deep reloads, native View Transitions, speaker sync, HMR, and static mounts in Chromium."}
    </p>
    <span>browser / E2E</span>
  </div>
</div>

<Note>
  Unit tests protect algorithms; browser tests protect the product. The highlighted final row
  reflects Drever's emphasis on real E2E evidence without replacing focused unit coverage.
</Note>

---

<Statement
  eyebrow="The design rule"
  index="11"
  title="Make agreement the default."
  supporting="One semantic deck. One canonical position. Explicit ownership. Small boundaries that compose."
/>

<Note>
  Close by returning to agreement. The architecture is successful when authors can think about the
  story while every delivery surface remains predictably consistent.
</Note>
