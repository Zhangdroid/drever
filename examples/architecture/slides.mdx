import { RouteCompiler } from "./ArchitectureLab.tsx";
import {
  ArtifactLineage,
  BoundaryMap,
  BuildGraph,
  CommitProtocol,
  CompilerRail,
  FailureBoundary,
  InvariantMap,
  RenderedPreflight,
  StoryContract,
  StudioLoop,
  SurfaceDelivery,
  TestBoundaries,
} from "./ArchitectureVisuals.tsx";
import "../showcase-return.css";
import "./architecture.css";

<section aria-label="One story. Every surface agrees." className="arch-slide arch-opening">
  <div className="arch-opening__copy">
    <span className="arch-kicker">Drever / architecture</span>
    <h1>
      {"One story."}
      <br />
      <em>Every surface agrees.</em>
    </h1>
    <p>Drever turns reviewed intent into one inspectable contract before it reaches the room.</p>
  </div>
  <BuildGraph />
</section>

<Note>
  This is an architecture of agreement, not a package tour. Follow one story from a reviewable plan
  to every place where its meaning, position, and evidence must remain true.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading">
    <span className="arch-kicker">01 / The invariant</span>
    <h1>
      <span>First, decide</span>
      <br />
      <span>what must never drift.</span>
    </h1>
    <p>Story, slides, Steps, notes, and URL need one semantic center.</p>
  </header>
  <InvariantMap />
</section>

<Note>
  Without a shared contract, the audience, speaker, document, and export surfaces each invent a
  slightly different deck. Drever fixes meaning and position before those surfaces exist.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">02 / Story contract</span>
    <h1>Before source, the story earns approval.</h1>
  </header>
  <StoryContract />
  <MotionGroup
    className="arch-step-caption arch-step-caption--centered"
    flow="inline"
    intent="focus"
  >
    <Step at={1}>
      <p>The brief records audience, desired change, duration, language, and deck-wide density.</p>
    </Step>
    <Step at={2}>
      <p>
        The v2 plan keeps each slide's job, purpose, evidence, and anchor artifact—nothing visual.
      </p>
    </Step>
    <Step at={3}>
      <p>The Storyboard approves content and sequence before design and authoring begin.</p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance three times. The versioned drever.plan.json is a pre-authoring contract, not a compiler
  pass and not a production dependency. Version 2 deliberately defers per-slide density, layout, and
  motion so the development-only Storyboard reviews content and sequence without pretending the
  design already exists.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">03 / Artifact lineage</span>
    <h1>Each boundary leaves evidence behind.</h1>
  </header>
  <ArtifactLineage />
  <MotionGroup className="arch-step-caption" flow="inline" intent="replace">
    <Step at={1}>
      <p>Deck IR gives authored meaning exact, serializable source ranges.</p>
    </Step>
    <Step at={2}>
      <p>CompilePlan freezes extension order and public capability as JSON-safe data.</p>
    </Step>
    <Step at={3}>
      <p>The deck artifact seals positions, notes, components, and manifest identity.</p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance three times. MDX stays readable, Deck IR owns semantics and source evidence, CompilePlan
  owns resolved extension order, and the final artifact owns every valid runtime position.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">04 / Protected compilation</span>
    <h1 aria-label="Extensions can transform content—not the deck contract.">
      <span>Extensions transform content—</span>
      <br />
      <span>not the deck contract.</span>
    </h1>
  </header>
  <CompilerRail />
  <MotionGroup
    className="arch-step-caption arch-step-caption--centered"
    flow="inline"
    intent="focus"
  >
    <Step at={1}>
      <p>Fixed grammar reserves root slide boundaries before extensions run.</p>
    </Step>
    <Step at={2}>
      <p>Analysis records Deck IR and exact authored source fragments.</p>
    </Step>
    <Step at={3}>
      <p>A JSON-safe CompilePlan resolves Theme, plugins, order, and provenance.</p>
    </Step>
    <Step at={4}>
      <p>MDX 3 runs protected Remark, Rehype, and Recma boundaries.</p>
    </Step>
    <Step at={5}>
      <p>The finalizer rejects structural drift and seals the manifest.</p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance five times. The public pipeline uses MDX 3 and upstream Vite 8, while Drever owns fixed
  pagination grammar, extension ordering, protected structure, and final artifact identity.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading">
    <span className="arch-kicker">05 / Local coordination</span>
    <h1>
      <span>The browser directs.</span>
      <br />
      <span>The agent owns the work.</span>
    </h1>
    <p>Typed actions and bounded progress connect Studio to ordinary project files.</p>
  </header>
  <StudioLoop />
</section>

<Note>
  Studio never becomes a cloud editor or a second source of truth. Native, ACP, and portable journal
  adapters normalize only typed actions and public progress. Model credentials stay with the local
  agent; brief.md, drever.plan.json, slides.mdx, assets, and Git remain authoritative.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading">
    <span className="arch-kicker">06 / Extension boundaries</span>
    <h1>
      <span>Design changes expression.</span>
      <br />
      <span>Plugins change capability.</span>
    </h1>
    <p>The same deck contract stays at the center.</p>
  </header>
  <BoundaryMap />
</section>

<Note>
  Components can exist in both. A design component is composition vocabulary; a plugin component is
  the runtime face of added capability. Ownership resolves the apparent overlap.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">07 / Canonical position</span>
    <h1>One coordinate drives every surface.</h1>
  </header>
  <RouteCompiler />
</section>

<Note>
  Change surface, slide, and Step. The controls are derived from the compiled manifest. The URL is
  its deterministic projection, and production materializes the same position as a static file.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">08 / Native navigation</span>
    <h1>One platform transaction commits it.</h1>
  </header>
  <CommitProtocol />
  <MotionGroup
    className="arch-step-caption arch-step-caption--centered"
    flow="inline"
    intent="focus"
  >
    <Step at={1}>
      <p>Navigation API owns cancellation, focus, history, and manual scroll.</p>
    </Step>
    <Step at={2}>
      <p>React commits inside a typed document View Transition.</p>
    </Step>
    <Step at={3}>
      <p>BroadcastChannel publishes the committed position—never a half-state.</p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance three times. Drever targets current browsers directly: Navigation API, document View
  Transitions, BroadcastChannel, and ResizeObserver are one explicit platform contract rather than a
  stack of legacy fallbacks.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading">
    <span className="arch-kicker">09 / Delivery</span>
    <h1>One route loads one surface.</h1>
    <p>Portable static files keep deep links, metadata, and audience bundles honest.</p>
  </header>
  <SurfaceDelivery />
</section>

<Note>
  The route bootstrap dynamically imports audience, document, or speaker code after resolving the
  canonical URL. Export has its own isolated app. Every manifest position becomes a real file, and
  typed deck metadata produces language, canonical, Open Graph, Twitter, and icon markup.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">10 / Rendered preflight</span>
    <h1>Inspect every state—not one lucky frame.</h1>
  </header>
  <RenderedPreflight />
  <MotionGroup
    className="arch-step-caption arch-step-caption--centered arch-step-caption--evidence"
    flow="block"
    intent="focus"
  >
    <Step at={1}>
      <p>Exact Slide and Step routes settle at the configured canvas in isolated Chromium.</p>
    </Step>
    <Step at={2}>
      <p>
        Forward and reverse handoffs expose clipping, overlap, contrast, drift, and moving paint.
      </p>
    </Step>
    <Step at={3}>
      <p>
        A fingerprinted manifest links diagnostics to full states, transitions, and contact sheets.
      </p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance three times. drever check --rendered runs only after source safety and builds an isolated
  inspection app. Its receipt and evidence manifest share one build fingerprint, so stale captures
  cannot look current. Image and gradient contrast remain explicit review warnings rather than false
  passes.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">11 / Resilient draft</span>
    <h1>
      <span>A broken slide cannot erase</span>
      <br />
      <span>the last good deck.</span>
    </h1>
  </header>
  <FailureBoundary />
  <MotionGroup
    className="arch-step-caption arch-step-caption--centered"
    flow="block"
    intent="focus"
  >
    <Step at={1}>
      <p>
        A valid repair replaces the preview atomically; partial work never becomes the new baseline.
      </p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance once. Authored render errors stay inside their owning slide or deck surface. Studio keeps
  the last valid draft visible, gives the agent a precise source diagnostic, and publishes the
  repaired revision only after it becomes one coherent render again.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">12 / Evidence</span>
    <h1>Prove each promise where it can break.</h1>
  </header>
  <TestBoundaries />
  <MotionGroup
    className="arch-step-caption arch-step-caption--centered arch-step-caption--evidence"
    flow="block"
    intent="focus"
  >
    <Step at={1}>
      <p>Compiler tests protect serializable contracts—not generated JavaScript snapshots.</p>
    </Step>
    <Step at={2}>
      <p>Rendered checks protect every exact state at the real canvas.</p>
    </Step>
    <Step at={3}>
      <p>Browser and real-AI runs protect the experience at its outermost boundary.</p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance three times. Pure state, compiled artifacts, rendered geometry, and real user outcomes
  each need different evidence. Agents can read the same versioned context, current position, and
  check report without becoming another source of truth.
</Note>

---

<section aria-label="Agreement, by default." className="arch-slide arch-closing">
  <div className="arch-closing__graph">
    <BuildGraph resolved />
  </div>
  <div className="arch-closing__copy">
    <span className="arch-kicker">The result</span>
    <h1>
      {"Agreement,"}
      <br />
      <em>by default.</em>
    </h1>
    <p>One story. One position. Inspectable evidence at every boundary.</p>
    <span className="drever-example-exit">
      <a
        className="drever-example-exit__primary"
        data-drever-showcase-return=""
        href="https://drever.dev/docs/getting-started/"
      >
        Create your own <span aria-hidden="true">↗</span>
      </a>
      <a className="drever-example-exit__secondary" href="https://drever.dev/showcase/">
        Explore more examples <span aria-hidden="true">→</span>
      </a>
    </span>
  </div>
</section>

<Note>
  Return to the opening graph, now fully resolved. The architecture succeeds when people can shape
  the story while every delivery surface and every review tool agrees on the same explicit contract.
</Note>
