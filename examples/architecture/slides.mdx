import { RouteCompiler } from "./ArchitectureLab.tsx";
import {
  ArtifactLineage,
  BoundaryMap,
  BuildGraph,
  CommitProtocol,
  CompilerRail,
  FailureBoundary,
  InvariantMap,
  StaticTopology,
  TestBoundaries,
} from "./ArchitectureVisuals.tsx";
import "../showcase-return.css";
import "./architecture.css";

<section aria-label="One source. Every surface agrees." className="arch-slide arch-opening">
  <div className="arch-opening__copy">
    <span className="arch-kicker">Drever / architecture</span>
    <h1>
      {"One source."}
      <br />
      <em>Every surface agrees.</em>
    </h1>
    <p>Drever turns authored intent into one contract before it becomes a presentation.</p>
  </div>
  <BuildGraph />
</section>

<Note>
  This is an architecture of agreement, not a package tour. Follow one idea from readable source to
  every place it must remain true.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading">
    <span className="arch-kicker">01 / The problem</span>
    <h1>First, decide what must never drift.</h1>
    <p>Slide, Step, notes, and URL need one semantic center.</p>
  </header>
  <InvariantMap />
</section>

<Note>
  Without a shared contract, the audience, speaker, document, and export surfaces each invent a
  slightly different deck. Drever fixes the meaning before those surfaces exist.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">02 / The contract</span>
    <h1>Before pixels, the deck is data.</h1>
  </header>
  <ArtifactLineage />
  <MotionGroup className="arch-step-caption" flow="inline" intent="replace">
    <Step at={1}>
      <p>Deck IR gives authored meaning a serializable shape.</p>
    </Step>
    <Step at={2}>
      <p>The manifest freezes every valid runtime position.</p>
    </Step>
    <Step at={3}>
      <p>Runtime reads the contract. It never reverse-engineers the DOM.</p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance three times. The source remains readable, Deck IR owns semantics, the manifest owns valid
  positions, and runtime only interprets the sealed result.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">03 / The compiler</span>
    <h1>Five owned passes turn intent into certainty.</h1>
  </header>
  <CompilerRail />
  <MotionGroup
    className="arch-step-caption arch-step-caption--centered"
    flow="inline"
    intent="focus"
  >
    <Step at={1}>
      <p>Split reserves root-level slide boundaries before extensions run.</p>
    </Step>
    <Step at={2}>
      <p>Analyze builds Deck IR with exact source ranges.</p>
    </Step>
    <Step at={3}>
      <p>Resolve validates theme, plugins, order, and provenance.</p>
    </Step>
    <Step at={4}>
      <p>Compile runs protected Remark, Rehype, and Recma phases.</p>
    </Step>
    <Step at={5}>
      <p>Seal validates the result and emits one immutable manifest.</p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance five times. Boundary detection happens first, so plugin order can never change pagination.
  Each pass has one owner and one artifact to hand forward.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading">
    <span className="arch-kicker">04 / Extension boundaries</span>
    <h1>Design changes expression. Plugins change capability.</h1>
    <p>The same deck contract stays at the center.</p>
  </header>
  <BoundaryMap />
</section>

<Note>
  Components can exist in both. A design component is composition vocabulary; a plugin component is
  the runtime face of added capability. Ownership resolves the apparent overlap.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">05 / Canonical position</span>
    <h1>One coordinate drives every surface.</h1>
  </header>
  <RouteCompiler />
</section>

<Note>
  Change surface, slide, and Step. The URL is a deterministic projection of the manifest, and
  production materializes the exact same position as a static file.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">06 / Navigation</span>
    <h1>Navigation commits that coordinate once.</h1>
  </header>
  <CommitProtocol />
  <MotionGroup
    className="arch-step-caption arch-step-caption--centered"
    flow="inline"
    intent="focus"
  >
    <Step at={1}>
      <p>URL, React, and native capture enter one atomic update.</p>
    </Step>
    <Step at={2}>
      <p>The committed position publishes to speaker and audience together.</p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance twice. The deck owns native capture while React owns the state commit. Persistent Stage
  layers remain live, and no surface observes a half-finished position.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading">
    <span className="arch-kicker">07 / Static delivery</span>
    <h1>The coordinate becomes a real file.</h1>
    <p>Every valid deep link can reload without an application server.</p>
  </header>
  <StaticTopology />
</section>

<Note>
  This is generation, not an SPA fallback. Each route computes its deployment mount before assets
  activate, including when the deck lives below a subdirectory.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">08 / Owned failure</span>
    <h1>Failure is reversible—and explainable.</h1>
  </header>
  <FailureBoundary />
  <MotionGroup
    className="arch-step-caption arch-step-caption--centered"
    flow="block"
    intent="focus"
  >
    <Step at={1}>
      <p>A failed acquisition rolls back in reverse and points to authored source.</p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance once. Setup behaves as a transaction. Expected failures cross boundaries as structured
  diagnostics with stable codes, ownership, location, and an actionable message.
</Note>

---

<section className="arch-slide">
  <header className="arch-slide-heading arch-slide-heading--compact">
    <span className="arch-kicker">09 / Evidence</span>
    <h1>Prove each promise where it can break.</h1>
  </header>
  <TestBoundaries />
  <MotionGroup
    className="arch-step-caption arch-step-caption--centered arch-step-caption--evidence"
    flow="block"
    intent="focus"
  >
    <Step at={1}>
      <p>Compiler tests protect the semantic artifact—not generated JavaScript snapshots.</p>
    </Step>
    <Step at={2}>
      <p>Browser tests protect reloads, transitions, synchronization, and static mounts.</p>
    </Step>
  </MotionGroup>
</section>

<Note>
  Advance twice. Unit tests protect pure state. Compiler tests protect the contract. Browser tests
  protect the product where real platform behavior can fail.
</Note>

---

<section aria-label="Agreement, by default." className="arch-slide arch-closing">
  <div className="arch-closing__graph">
    <BuildGraph resolved />
  </div>
  <div className="arch-closing__copy">
    <span className="arch-kicker">The result</span>
    <h1>
      {"Agreement,"}
      <br />
      <em>by default.</em>
    </h1>
    <p>One story. One position. Every surface in sync.</p>
    <span className="drever-example-exit">
      <a
        className="drever-example-exit__primary"
        data-drever-showcase-return=""
        href="https://drever.dev/docs/getting-started/"
      >
        Create your own <span aria-hidden="true">↗</span>
      </a>
      <a className="drever-example-exit__secondary" href="https://drever.dev/showcase/">
        Explore more examples <span aria-hidden="true">→</span>
      </a>
    </span>
  </div>
</section>

<Note>
  Return to the opening graph, now fully resolved. The architecture succeeds when authors can focus
  on the story while every delivery surface remains predictably consistent.
</Note>
