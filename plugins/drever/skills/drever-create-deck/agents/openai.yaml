# Generated by `drever agent sync`.
interface:
  display_name: "Drever Create Deck"
  short_description: "Open an early Drever draft, then refine and validate it"
  default_prompt: "Use $drever-create-deck to turn this brief into an early live draft, then refine it into a validated Drever presentation."
