# Generated by `drever agent sync`.
interface:
  display_name: "Drever Create Deck"
  short_description: "Review the plan, then create and validate a Drever deck"
  default_prompt: "Use $drever-create-deck to write a reviewable plan and slide outline to brief.md, wait for my approval, then create and validate the Drever presentation."
